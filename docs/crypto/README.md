# QPB Crypto Notes

- **ML-DSA-65 (alg_id 0x11)** — consensus-active at genesis. See `docs/crypto/MLDSA-65.md` for sizes, provenance, and validation invariants.
- **SLH-DSA (alg_id 0x21)** — reserved/inactive; rejected by consensus.
- **SHRINCS (alg_id 0x30)** — reserved/inactive; dev-only research stubs; rejected by consensus.

Future alg_id activations require explicit hard fork and spec/code updates.
